# Deployment

The existing Vite build copies this directory from `public/` into the deployed GitHub Pages artifact. After approval and merge to `main`, the archive will be available at:

`https://aintfunny18-tech.github.io/f4-prestige-archive/heist-crew-archive/`

The current branch does not alter the existing prestige-class application or its routes.
